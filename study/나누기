// 두 수 입력 후 나누고 소수점 출력
#include <iostream> 

using namespace std;

int main() {

	double A, B;
	
	cin >> A;
	cin >> B;
	cout.precision(10);
	// cout.precision(n); 소수점 n번까지 나타냄
	cout << A / B;

	return 0;
}