#include <iostream>
#include <vector>
using namespace std;

int main() {

	int a, b;
	vector<int> arr;
	int count = 0;

	while (cin >> a >> b) {
		arr.push_back(a + b);
		count += 1;
	}

	for (int i = 0; i < count; i++) {
		cout << arr[i] << '\n';
	}
	
	
	return 0;
}