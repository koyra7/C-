#include <iostream>
#include <string>

using namespace std;

int main() {

	string A, B, C;

	cin >> A >> B >> C;
	
	int num;
	num = stoi(A) + stoi(B) - stoi(C);
	
	string ch ="";
	ch += A;
	ch += B;
	
	int sum = stoi(ch) - stoi(C);

	cout << num << '\n' << sum;

	return 0;
}