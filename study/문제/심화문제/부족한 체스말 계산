#include <iostream>
#include <vector>

using namespace std;

int main() {

	vector<int> va = { 1,1,2,2,2,8 };

	vector<int> in(6);

	for (int i = 0; i < 6; ++i) {
		cin >> in[i];
	}

	for (int i = 0; i < 6; ++i) {
		cout << va[i] - in[i] << ' ';
	}

	return 0;
}