#include <iostream>
#include <vector>
using namespace std;

int main() {

	int X,Y;
	vector<int> arr;
	int number;

	cin >> X >> Y;

	for (int i = 0; i < X; i++) {
		cin >> number;
		if (number < Y) {
			arr.push_back(number);
		}
	}
	
	for (int i = 0; i < arr.size(); i++) {
		cout << arr[i] << " ";
	}

	return 0;
}